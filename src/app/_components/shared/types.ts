// Shared types are intentionally inferred from the tRPC router output.
// If you need explicit entity types, derive them from your query results
// using `typeof queriedData[number]` in each component.
//
// This file is reserved for types shared across multiple tab components.
